{{> menu active="dashboard"}}
<section class="section">
    <h3 class="title">
      Stations
    </h3>
    {{#if user}}
        {{> map }}
        {{> add-station}}
        {{> list-stations}}
    {{else}}
    <div class="field is-grouped">
      <a href="/user" class="button is-link">Login</a>
    </div>
    {{/if}}
</section>
